<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    
    
<mapper namespace="boardNotice">

	<!-- 시퀀스 생성 -->
	<select id="sequence" resultType="int">
		select notice_seq.nextval from dual
	</select>
	<!-- Create  -->
	<insert id="save">
		insert into board_notice (
			notice_no, company_no, notice_title, notice_content
		)
		values (#{noticeNo}, #{companyNo}, #{noticeTitle}, #{noticeContent})
	</insert>
	
	
	
	<!-- Read  -->
	<select id="list" resultType="boardNoticeDto">
		select * from board_notice where company_no = #{companyNo} order by notice_no desc
	</select>
	<select id="find" resultType="boardNoticeDto">
		select * from board_notice where notice_no = #{noticeNo}
	</select>
	
	<!-- update -->
	<update id="editUnit">
	    update board_notice
		    <set>
		        <if test="noticeTitle != null">notice_title = #{noticeTitle},</if>
		        <if test="noticeContent != null">notice_content = #{noticeContent},</if>
		        notice_etime = sysdate 
		    </set>
	    where notice_no = #{noticeNo}
	</update>
	

</mapper>